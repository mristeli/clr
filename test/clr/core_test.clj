(ns clr.core-test
  (:require [clojure.test :refer :all]
            [clr.core :refer :all]))

;(deftest a-test
;  (testing "FIXME, I fail."
;    (is (= 0 1))))

;; (deftest sum-test
;;   (testing "Sum fails"
;;     (is (sum '(1 3 4)) 8)))
